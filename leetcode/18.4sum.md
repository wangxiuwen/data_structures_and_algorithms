# [18. 四数之和](https://leetcode-cn.com/problems/4sum/)

给定一个包含 n 个整数的数组 `nums` 和一个目标值 `target`，判断 `nums` 中是否存在四个元素 a，b，c 和 d ，使得 a + b + c + d 的值与 `target` 相等？找出所有满足条件且不重复的四元组。

注意：

答案中不可以包含重复的四元组。

示例：

```
给定数组 nums = [1, 0, -1, 0, -2, 2]，和 target = 0。

满足要求的四元组集合为：
[
  [-1,  0, 0, 1],
  [-2, -1, 1, 2],
  [-2,  0, 0, 2]
]
```


## 解法一

```Java
class Solution {
    public List<List<Integer>> fourSum(int[] nums, int target) {
        
        if (nums == null || nums.length < 4) return new ArrayList<>();
        int n = nums.length;

        List<List<Integer>> result = new ArrayList<>();

        Arrays.sort(nums);
        //p, q, i, j
        for (int p = 0; p < n - 3; p++) {

            if (p > 0 && nums[p] == nums[p - 1]) continue;

            for (int q = p + 1; q < n - 2; q++) {
            
                if (q > p + 1 && nums[q] == nums[q - 1]) continue;

                int i = q + 1;
                int j = n - 1;

                while (i < j) {
                    int sum3 = nums[p] + nums[q] + nums[i] + nums[j];
                    if (sum3 == target) {
                        result.add(Arrays.asList(nums[p], nums[q], nums[i], nums[j]));
                        i++;
                        j--;
                        while(i < j && nums[i] == nums[i - 1]) i++;
                        while(i < j && nums[j] == nums[j + 1]) j--;
                    } else if (sum3 > target) {
                        j--;
                        while (i < j && nums[j] == nums[j + 1]) j--;
                    } else {
                        i++;
                        while(i < j && nums[i] == nums[i - 1]) i++;
                    }
                }
            }
        }
        return result;
    }
}
```

优化

```Java
class Solution {
    public List<List<Integer>> fourSum(int[] nums, int target) {
        
        if (nums == null || nums.length < 4) return new ArrayList<>();
        int n = nums.length;

        List<List<Integer>> result = new ArrayList<>();

        Arrays.sort(nums);
        //p, q, i, j
        for (int p = 0; p < n - 3; p++) {

            if (p > 0 && nums[p] == nums[p - 1]) continue;

            int min1 = nums[p] + nums[p + 1] + nums[p + 2] + nums[p + 3];
            if (min1 > target) break;

            int sum1 = nums[p] + nums[n - 1] + nums[n - 2] + nums[n - 3];
            if (sum1 < target) continue;

            for (int q = p + 1; q < n - 2; q++) {
            
                if (q > p + 1 && nums[q] == nums[q - 1]) continue;

                int min2 = nums[p] + nums[q] + nums[q + 1] + nums[q + 2];
                if (min2 > target) break;

                int sum2 = nums[p] + nums[q] + nums[n - 1] + nums[n - 2];
                if (sum2 < target) continue;

                int i = q + 1;
                int j = n - 1;

                while (i < j) {
                    int sum3 = nums[p] + nums[q] + nums[i] + nums[j];
                    if (sum3 == target) {
                        result.add(Arrays.asList(nums[p], nums[q], nums[i], nums[j]));
                        i++;
                        j--;
                        while(i < j && nums[i] == nums[i - 1]) i++;
                        while(i < j && nums[j] == nums[j + 1]) j--;
                    } else if (sum3 > target) {
                        j--;
                        while (i < j && nums[j] == nums[j + 1]) j--;
                    } else {
                        i++;
                        while(i < j && nums[i] == nums[i - 1]) i++;
                    }
                }
            }
        }
        return result;
    }
}
```

